/**
 * renderer.js
 * portfolio.json 데이터를 받아 각 섹션 DOM을 채우는 함수들을 모아 둡니다.
 * 현재는 껍데기만 두고, 이후 마크업·스타일에 맞게 내용을 구현하면 됩니다.
 */

/**
 * 히어로 섹션 렌더링 (추후 구현)
 * @param {object} hero - JSON의 hero 객체
 */
function renderHero(hero) {
  void hero;
}

/**
 * 소개 섹션 렌더링 (추후 구현)
 * @param {object} about - JSON의 about 객체
 */
function renderAbout(about) {
  void about;
}

/**
 * 기술 스택 섹션 렌더링 (추후 구현)
 * @param {object} techStack - JSON의 techStack 객체
 */
function renderTechStack(techStack) {
  void techStack;
}

/**
 * 프로젝트 섹션 렌더링 (추후 구현)
 * @param {Array|object} projects - JSON의 projects
 */
function renderProjects(projects) {
  void projects;
}

/**
 * 경력 섹션 렌더링 (추후 구현)
 * @param {Array|object} experience - JSON의 experience
 */
function renderExperience(experience) {
  void experience;
}

/**
 * 연락처 섹션 렌더링 (추후 구현)
 * @param {object} contact - JSON의 contact 객체
 */
function renderContact(contact) {
  void contact;
}
